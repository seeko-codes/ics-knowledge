for this entire session I want you to be using 4 files (logic, concepts, details, and provenance). 

I want your general training response to be secondary for this entire session I want you to be using these folders exclusively.

Let me familiarize you with the landscape:

The logic file will give you your lens. It's the first thing you should consider when fetching a response to me as they're your guiding principles. The mechanisms I believe underpin every other folder, your (why).

The provenance file sits at the same level of abstraction as logic, not below details. Its role is to map ICS (our source) — that mapping function is inherently high-level, since it's tracing where the whole tree's claims come from, not a tactic or an action step. Consult it alongside logic when you need to ground a response in what ICS actually says.

The concept file will give you the common manifestations of how the primary principles show up. These land the plane.

The details file will give you tactics (only fetch this when trying to give me a particular action plan).

Each of these files in order grow more and more dense as their ontology is really just a rooted tree representing their abstraction. Logic and provenance sit at the highest level of abstraction (why and source), concepts second, details third. And so when you go down from one level of abstraction to the next, you'll naturally find plenty examples for the same talking point in the file above.

Actual counts (as of 2026-08-23): 01-logic = 11 files, 02-concepts = 37 files, 03-details = 53 files (10 tables + 5 examples + 38 techniques), 99-provenance = 17 files (6 top-level + 11 inventory).

There is also a `feedback.md` file at the project root — an error log of corrections I give you during sessions. Read it at the same time as the details layer: it records where a past response went wrong (or where a claim was mislabeled/overstated) and why, so the same mistake doesn't recur. Append a new entry to it whenever I correct something.

The idea here is for logic and provenance together to narrow the search space of the concepts, and for that to narrow the search space for the technique files. This is will be the flow of your queries here.

This is directionality is important for two reasons:
  1. it narrows the search space in an effective manner this way.
  2. it avoids using abstract LLM terminology as for delivery (last step) grounds your response in the way these topics are actually talked about.